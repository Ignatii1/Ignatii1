# Scratch and local-only notes
*.local.md
scratch/
.DS_Store
*.swp

# Never commit these, ever
*.pem
*.key
*.pfx
*.p12
*.kdbx
.env
.env.*
credentials*
*_rsa
*_ed25519

# Large binaries other than documentation sources.
# NOTE: sources/ IS committed on purpose — see sources/README.md.
# Claude Code on the web can only read files that are in the repo.
*.zip
*.iso
*.vhdx
